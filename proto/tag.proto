syntax = "proto3";

message GetTagRequest {
    //没参数
}

message GetTagResponse {
    int32 status_code = 1; //状态码，0是成功，其它是错误码
    string message = 2; // 错误信息，0时为success
    repeated string tags = 3;//tag列表
}